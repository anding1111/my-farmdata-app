import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

const BatchesTab = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Lotes</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Módulo de lotes en desarrollo.</p>
      </CardContent>
    </Card>
  );
};

export default BatchesTab;